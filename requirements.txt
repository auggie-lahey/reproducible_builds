# Python dependencies for reproducible build attestation system

# YAML parsing
PyYAML>=6.0

# HTTP requests
requests>=2.31.0

# Nostr event signing and publishing (via nak CLI tool)
# Install nak separately: https://github.com/fiatjaf/nak
